import NavBar from '../components/Navbar';

const Settings = (props) => {
    return ( 
       <div>
            <NavBar/>
            <h1 className="text-white font-bold top-14 left-60 absolute">Settings</h1>
       </div> 
    );
}
 
export default Settings;